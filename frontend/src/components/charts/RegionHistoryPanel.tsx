import { useEffect,useState } from "react";
import { api } from "../../services/api";
import "./region-history.css";
type EventRow={year:number|null;hazard:string;record:Record<string,unknown>};
export default function RegionHistoryPanel({region,district}:{region:string;district?:string}){const[data,setData]=useState<{events:EventRow[];hazard_counts:Record<string,number>;dataset_period?:string;source?:string}|null>(null);const[articles,setArticles]=useState<any[]>([]);useEffect(()=>{let active=true;setData(null);setArticles([]);Promise.all([api.history.region(region,district),api.live.news(region,district)]).then(([h,n])=>{if(active){setData(h);setArticles(n.articles||[])}}).catch(()=>active&&setData({events:[],hazard_counts:{}}));return()=>{active=false}},[region,district]);if(!region)return null;const hazards=Object.entries(data?.hazard_counts||{}).sort((a,b)=>b[1]-a[1]);const max=Math.max(1,...hazards.map(([,v])=>v));return <section className="region-history-panel panel"><div className="section-heading"><div><span className="eyebrow">HISTORICAL INCIDENT DATA</span><h2>{district||region} History</h2></div><span>{data?.events.length||0} records</span></div><div className="history-analytics-grid"><div className="history-chart"><strong>Recorded hazard types</strong>{hazards.length?hazards.map(([hazard,count],i)=><div className="hazard-row" key={hazard}><span>{hazard}</span><div><i className={`chart-bar bar-${i%5}`} style={{width:`${Math.max(8,(count/max)*100)}%`}}/></div><b>{count}</b></div>):<p>No source records loaded.</p>}</div><div className="incident-list"><strong>Historical source records</strong>{data?.events.slice(0,8).map((e,i)=><article key={`${e.year}-${e.hazard}-${i}`}><div><b>{e.year??"—"}</b><span>{e.hazard}</span></div><small>Source dataset record · no severity or impact value inferred</small></article>)}</div></div><div className="news-panel"><div className="inline-title"><h3>Current regional news</h3><span>Google News publisher results</span></div>{articles.slice(0,6).map((a,i)=><a className="news-row" href={a.url} target="_blank" rel="noreferrer" key={`${a.url}-${i}`}><b>{a.title}</b><small>{a.source||a.domain||"Publisher"}</small></a>)}{!articles.length&&<p>No current news results returned.</p>}</div><p className="data-note">Historical records come from the open IFI-Impacts flood inventory. Current publisher news is fetched separately for the selected district.</p></section>}
